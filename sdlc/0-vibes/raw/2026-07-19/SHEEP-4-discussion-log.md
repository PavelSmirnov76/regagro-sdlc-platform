Полный лог обсуждения разбора `SHEEP-4.md` по `sdlc/`, промпты пользователя
(Павел Смирнов) и ответы ассистента, по порядку. Ведётся с этой даты,
пополняется по ходу работы над тикетом.

---

## Промпт 1 (пользователь)

> Закончил создание sdlc первых шагов. Спеки. Теперь хочу протестировать, как
> ты будешь выполнять задачи по этой спеке. Вот первый пул задач. @SHEEP-4.md
>
> Отработай по нему, так как нужно по @sdlc
>
> Лучше наверное задачи делать по одной.

## Ответ 1 (ассистент)

Изучил `sdlc/RUNBOOK.md`/`AGENTS.md`, текущий `PRD.md`, `1-business-tasks/`,
`2-specs/` (модуль ANIMAL, группы REG/IDENT). Собрал контекст по пункту 1
чек-листа (QR визуальной бирки): нашёл, что поле «Визуальная бирка» есть
только у шага «Маркирование», что валидация этого поля уже отключена в
коде (`RINTAGLE-395`), и что в приложении уже есть переиспользуемые
QR-сканер-компоненты (`QRScanner`, `PassportQRScanner`). Задал уточняющий
вопрос через `AskUserQuestion`: как трактовать критерий приёмки тикета
«действующие проверки формата и уникальности номера» против того факта, что
валидация в коде сейчас отключена.

## Промпт 2 (пользователь, ответ на AskUserQuestion)

> Давай соберем отдельный файл противоречий по каждому пункту. И рассматриваем
> их все и делаем отдельно. По поводу первого таска
>
> 1. При ошибке или невозможности распознавания отображается понятное
>    сообщение. это делать не нужно. В любом случае мы либо просканируем,
>    либо нет.
> 2. Система выполняет действующие проверки формата и уникальности номера.
>    Это не нужно. Ранее обсуждалось с заказчиком, что в инпуте визуальной
>    бирки может быть что угодно. QR считываем ровно то, что там записано. Не
>    правильная постанова задачи.

## Ответ 2 (ассистент)

Завёл `0-vibes/raw/2026-07-19/SHEEP-4.md` (тикет целиком, verbatim) и
`SHEEP-4-clarifications.md` (файл разногласий по пунктам, начат с пункта 1).
Прошёл пункт 1 через весь пайплайн: `BT-17-PLANNING-ANIMAL-REG-QR-VISUAL-TAG`
(служит `R9`, PRD не меняется), `UC-52` заменён на `UC-305` (TARGET —
QR-сканирование бирки), индекс `use-cases/README.md` обновлён, дизайн-стадия
пропущена (нет доступной Figma), задача `4-tasks/SHEEP-4-1-QR-VISUAL-TAG-SCAN.md`
заведена как запись учёта (трекер не подключён). Предложил перейти к пункту 2
тем же порядком.

## Промпт 3 (пользователь)

> отлично.По всем пунктам. Фигмы не будет. Дизайна нового нет. Просто делаем
> правки уже готовых экранов. Максимум можем обсудить словами. Какого-то
> нового дизайна делать не нужно, пока не обсудим.
>
> По пункту 1 зафиксируй, что нужно посмотреть
> @lib/pages/weigh_animal/widgets/scanner_widget.dart и взять иконку там, а
> логику сканирования ищи. ВРоде тоже там есть или в PassportQRScanner
> QRScanner. Только смотри, что-бы сканер был нейтральным и не было
> упоминаний паспорта.
>
> Пункт 2. Изучи его на готовность. Может он уже частично сделан.

## Ответ 3 (ассистент)

Прочитал `lib/pages/weigh_animal/widgets/scanner_widget.dart` — нашёл
готовый нейтральный `ScannerWidget` (иконка `Assets.scanner`, логика
«настройка «камера как QR» → аппаратный скан с fallback на камеру», без
упоминаний паспорта), уже переиспользуемый в других экранах.
Скорректировал пункт 1 по правилу frozen-артефактов: `UC-305` → entomb →
`UC-306` (TARGET ссылается на `ScannerWidget`, не на `PassportQRScanner`),
`BT-17` → entomb → `BT-18`, задача в `4-tasks/` переписана (mutable, правка
in place). Добавил в `SHEEP-4-clarifications.md` общий пункт «дизайна не
будет по всему тикету».

Исследовал пункт 2 (`lib/pages/animals_inventory/`,
`InventoryReportDetailsCubit`/`InventoryAccordionListWidget`): нашёл, что
секция «Чужие метки» уже реализована почти полностью (`placeName`,
`ageGroupName`, номер, тот же заголовок, что в тикете). Отчитался о двух
предполагаемых расхождениях: (1) fallback при отсутствии места — `'-'`
вместо текстового значения; (2) вид животного показывается только если нет
возрастной группы. Предложил оформить BT/UC/task на эту дельту.

## Промпт 4 (пользователь)

> В пункте 2. Всегда говорим отделение. Место содержания устаревшее понятие и
> ошибка в таске.
> 1. Дааай указывать "отделение не указано" если нет места. В том числе если
>    номер вообще не найдет среди животных (там есть и такой флоу, тогда там
>    вообще сейчас ничего кроме номера не указывается. Подтверди, что нашел.
> 2. ТУт ошибка в таске. возрастная группа важнее а вид показывать только
>    если возростной группы нет. Тут у унас все ок.
>
> И еще попрошу тебя фиксировать все мои промты и все твои ответы в отдельный
> md файл. Что-бы у меня был файл с полным обсуждением задачи.

## Ответ 4 (ассистент)

Подтвердил находку второго флоу (`state.otherAnimals`/`unknownNumbers`,
`_computeSections` :67,:166, рендер `_UnknownNumbersWrap` — сейчас только
голый номер, без метки об отделении). Принял правку терминологии:
«отделение», не «место содержания» — второе устарело и было ошибкой в моей
формулировке, не в самом коде (код использует `Place`, ошибка была только в
моём русском тексте тикета/выводов). Откатил пункт про приоритет
вид/возрастная группа — это не баг, а корректное текущее поведение. Завёл
этот файл (`SHEEP-4-discussion-log.md`) для непрерывного лога.

Оформил пункт 2 по пайплайну с учётом обеих правок:
`BT-19-PLANNING-ANIMAL-INV-FOREIGN-TAG-DEPARTMENT` (служит `R17`, PRD не
меняется); `UC-176`/`UC-178` заменены (entomb+supersede) на `UC-307`/`UC-308`
— TARGET описывает текстовый fallback «Отделение не указано» в двух местах
(известная чужая метка без места; номер, не найденный ни в одной
идентификации), приоритет вид/возрастная группа явно зафиксирован как
неизменный; индекс `use-cases/README.md` обновлён; задача
`4-tasks/SHEEP-4-2-INV-FOREIGN-TAG-DEPARTMENT.md` заведена, с пометкой, что
нужен новый ключ локализации (`/add-translation`) и что точный layout строки
для «неизвестных» номеров не задан макетом — решает исполнитель либо
согласуется словами.

---

## Промпт 5 (пользователь)

> Переходим к пункту 3. Задавай вопросы, если нужно

## Ответ 5 (ассистент)

Запустил фоновый ресёрч по пункту 3 (экран отчёта по перемещению, эталонный
переход в карточку животного, наличие экрана «электронный паспорт»,
сохранение прокрутки/состояния, проверка прав, охват профилей). Агент нашёл:
список животных в отчёте по перемещению рендерится общим виджетом
`EventReportBody`/`EventReportGroup`, используемым также отчётами по
вакцинации и выбытию; `animalId` нигде не хранится, строки не тапабельны;
эталонный переход уже есть в `registration_day_report_view.dart`
(`context.pushNamed2(Routes.animalDetailsFromSearch, extra:
AnimalCardExtra(animalId: ...))`); отдельного экрана «электронный паспорт» в
коде нет; проверок прав на аналогичных переходах нет нигде.

Задал три уточняющих вопроса через `AskUserQuestion`. Ответы: (1) «карточка»
и «электронный паспорт» — один и тот же экран, не два; (2) критерий про
проверку прав — ошибка в тикете, не строится; (3) правка касается всех трёх
отчётов (перемещение/вакцинация/выбытие), раз компонент общий, а не только
«Перемещения», как буквально написано в тикете.

Оформил по пайплайну: `BT-20-PLANNING-ANIMAL-EVENT-REPORT-ANIMAL-CARD-LINK`
(служит `R24`). По ходу написания текста задачи сам поймал и исправил
собственную ошибку: изначально приписал вакцинационному дневному отчёту
неверную ссылку на `UC-111` — перепроверкой выяснилось, что
`UC-111` это другой сценарий (список вакцинаций одного животного,
`AnimalVaccinationsCubit`), не день-репорт. Поправил `BT-20` до финализации, не
показывая пользователю ошибочную версию.

`UC-149`/`UC-109`/`UC-163` заменены (entomb+supersede) на
`UC-309`/`UC-310`/`UC-311` — TARGET во всех трёх идентичен по сути (тап →
`AnimalCardPage`, `push`, без проверки прав, без отдельного экрана
паспорта), различаются только исходный cubit/сущность. Индекс
`use-cases/README.md` обновлён. Задача
`4-tasks/SHEEP-4-3-EVENT-REPORT-ANIMAL-CARD-LINK.md` заведена, одна на все
три отчёта (общий компонент — общая задача).

---

## Промпт 6 (пользователь)

> переходим к 4 пункту.

## Ответ 6 (ассистент)

Изучил `AnimalCardWidget`/`AnimalsList` (общий для «Реестра животных» и
основного списка животных) и два уже существующих приватных виджета
«раскраски последних 4 цифр» (`_HighlightedNumber` в отчётах,
`_HighlightedNumberText` в инвентаризации) — оба показывают номер целиком,
не обрезают, в отличие от буквального примера тикета (`…6133`). Задал два
уточняющих вопроса: (1) реальная обрезка с многоточием или переиспользование
раскрашивающего паттерна без обрезки; (2) применять правку в обоих экранах
(реестр + основной список) или только в реестре, раз компонент общий, как в
пункте 3. Ответы: (1) расширяем `HighlightedNumber` флагом `isShort`
(`true` — обрезка «…XXXX», `false` — текущее поведение без изменений); (2)
в обоих экранах.

Оформил по пайплайну: `BT-21-PLANNING-ANIMAL-COMPACT-NUMBER-DISPLAY`
(служит `R8`). Заодно перепроверил критерии тикета про поиск/сортировку по
полному номеру — в реестре текстового поиска нет вообще, критерии
неприменимы, зафиксировано явно, а не молча проигнорировано. `UC-71`
(основной список)/`UC-73` (реестр) заменены (entomb+supersede) на
`UC-312`/`UC-313` — TARGET описывает `isShort: true` для этих двух экранов,
поведение существующих потребителей паттерна (`UC-307`–`UC-311`) явно
зафиксировано как неизменное. Индекс `use-cases/README.md` обновлён. Задача
`4-tasks/SHEEP-4-4-COMPACT-NUMBER-DISPLAY.md` заведена, с рекомендацией не
плодить третий приватный дубликат виджета.

---

## Промпт 7 (пользователь)

> пункт 5 не нужен. Пропускаем

## Ответ 7 (ассистент)

Пункт 5 («дополнительное средство маркирования») пропущен по прямому решению
постановщика — сам тикет уже помечал его как требующий согласования с бэком.
Зафиксировано в `SHEEP-4-clarifications.md`. `BT`/`UC`/`4-tasks` не
заводятся. Перехожу к пункту 6.

---

## Промпт 8 (пользователь)

> да. Переходим к пункт 6

## Ответ 8 (ассистент)

Запустил фоновый ресёрч по пункту 6 (экран профиля, модель `User`, наличие
данных об организации, существующие read-only паттерны). Агент нашёл:
`User.organization: Organization?` уже полностью парсится и сохраняется в
Hive при каждом логине (поля name/inn/kpp/директор/телефон/email/реестровые
коды), но нигде не отображается; есть мёртвый ключ локализации
`profile_settings__organization_name`; адреса как поля в `Organization` нет
вообще; «роль в организации» не покрывается ни одним существующим полем
(ближайший кандидат — `User.roles`, но это системная роль, не должность в
организации); эталонный read-only паттерн — `ListInfoTile` (уже используется
в карточке вакцинации).

Задал 4 уточняющих вопроса. На первый же (какие поля показывать) получен
ответ, снимающий всю задачу:

> Пропускаем этот таск, помечаем, что требуется задача на бэк. Сейчас
> Организация не приходит с бэка

Пункт 6 пропущен — зафиксировано в `SHEEP-4-clarifications.md` вместе с
находками агента, чтобы при появлении задачи на бэк было от чего оттолкнуться
на фронтенде. `BT`/`UC`/`4-tasks` не заводятся.

---

## Промпт 9 (пользователь)

> переходим к пункту 7

## Ответ 9 (ассистент)

Запустил фоновый ресёрч по пункту 7 (экран настроек RFID-считывателя, модель
мощности/региона, реальный SDK устройства, логика региона по умолчанию).
Агент нашёл: переименование «Мощность» — чисто строковая правка ARB-ключа
`power_range`; регионы (`DeviceRegion`, 10 значений SDK читалки GReader)
сейчас подписаны нелокализованными техническими строками без стран; есть
брошенный черновик локализации на 4 региона. Задал 3 уточняющих вопроса.

По ходу проверки критерия «применяются после переподключения» сам копнул
глубже (`ScannerService.applySettings()`) и обнаружил, что функция вообще
ничего не делает для всех типов устройств, кроме TCD — запустил второй,
более прицельный ресёрч, чтобы точно картировать применение настроек по
каждому из 6+ типов устройств, а не описать в спеке нечто неточное.

Второй агент подтвердил и уточнил: TCD и SMART-GATE BLE применяют
мощность/регион на подключении корректно; SMART-GATE TCP и RFID-04 TCP —
баг (регион-свитч покрывает только индексы 0–3, дефолтный ETSI=4 тихо
уходит на китайский диапазон); RFID-03 handheld — мощность/регион вообще
никогда не передаются на устройство (нет кода вообще); A7/UHF-стик — не
применимо.

Пользователь спросил прямо, на основании чего я решил, что приложение
работает только в России (я упомянул это как контекст к вопросу об охвате
регионов) — ответил прямо: основание было слабым (`Constants.isRint`,
`bundleId`), а 9 языковых `.arb`-файлов прямо ему
противоречат; признал вывод необоснованным. Итоговые ответы: (1) чинить оба
бага в этом же проходе вместе с текстом; (2) подписать все 10 регионов
странами, не только 4 ключевых; (3) предупреждение о несовпадении региона —
не строить, отдельная задача с обсуждением дизайна.

Оформил по пайплайну: `BT-22-PLANNING-PROFILE-RFID-SETTINGS` (служит `R25`).
Для найденных багов заведено новое `EVT-148` (подключение сканера → перенос
сохранённых настроек на устройство — раньше не было своего события, только
частично покрывалось `EVT-136`, только для TCD и только sync-путём) и
`UC-314` (`UPDATE_OK`), CURRENT документирует оба дефекта по каждому типу
устройства, TARGET их исправляет, не трогая уже корректные TCD/SMART-GATE
BLE. Индексы `events/README.md`/`use-cases/README.md` обновлены. Задача
`4-tasks/SHEEP-4-7-RFID-SETTINGS.md` заведена — с полной таблицей
страна/регион на все 10 значений и явным списком того, что сознательно не
делается (предупреждение, чистка мёртвого кода).

---

## Промпт 10 (пользователь)

> переходи к пунтку 8

## Ответ 10 (ассистент)

Запустил ресёрч по пункту 8 (шаг «Название» визарда регистрации фермы).
Агент нашёл: `canProceedToNextStep()` для шага названия всегда `true` —
проверки нет вообще; текстовое поле шага не поддерживает `validator` даже
структурно (не прокинут в конструкторе, хотя нижележащий виджет его
поддерживает); кнопка при невозможности продолжить просто исчезает
(`SizedBox.shrink()`), не подсвечивает поле и не показывает сообщение;
клавиатура перекрывает `floatingActionButton`, т.к. `resizeToAvoidBottomInset:
false` без компенсации именно для кнопки; двойного тапа на сохранении
ничего не блокирует. Сохранение названия при возврате на предыдущий шаг —
уже работает корректно, чинить не нужно. Нашёл готовые эталонные паттерны
в проекте под все три правки (`Form`+`validator`+`isSubmitting` в другом
экране регистрации) — переиспользуются, не изобретаются
заново. Заодно вскрылось: существующая спека `UC-1` ссылалась на мёртвый
`FarmsAndPlacesBloc._onAddFarm` (ни разу не вызывается ни из какого UI) —
устарела относительно текущего визард-кода ещё до этого прохода.

Вопросов не потребовалось — все решения прямо следуют уже существующим в
проекте паттернам, без новых продуктовых развилок. Оформил по пайплайну:
`BT-23-PLANNING-FARM-NAME-VALIDATION-KEYBOARD` (служит `R5`). `UC-1`
заменён (entomb+supersede) на `UC-315` — TARGET описывает trim-aware
валидацию с подсветкой поля, починку клавиатуры на шаге названия и
`isSubmitting`-защиту от двойного тапа на сохранении; заодно исправлена
устаревшая ссылка на мёртвый обработчик. Индекс `use-cases/README.md`
обновлён. Задача `4-tasks/SHEEP-4-8-FARM-NAME-VALIDATION-KEYBOARD.md`
заведена.

---

## Промпт 11 (пользователь)

> пункты 9 и 10 тоже нужны.
>
> пункт 9. Проблема в том. что повторное взвешивание одного и того же
> животного создает новую запись. Если сегодня уже было взвешивание, то
> нужно редактировать сегодняшную запись. Посмотри как это сделано в другом месте
>
> пункт 10. Нужно краткое исследование, коректно ли данные записываются в
> бд и корректно ли передаются на бэк и корректно ли передаются на
> устройство (через native chanel)

## Ответ 11 (ассистент)

Указал, что в чек-листе тикета на самом деле 10 пунктов, а не 8 — последние
два без развёрнутого описания в теле тикета. Пользователь подтвердил, что
оба нужны, и дал контекст для каждого напрямую (не из текста тикета).

Запустил два параллельных фоновых ресёрча.

**Пункт 9** (агент): дедуп «сегодняшней записи» уже реализован и работает,
фикс уже смержен в `develop` до даты тикета (git-история)
— доложил как «похоже, уже исправлено», спросил, воспроизводится ли сейчас.

**Пункт 10** (агент): нашёл реальный, прослеживаемый баг — правая кнопка
ТСД по умолчанию настроена на `uhfPlusQrScanner`, нативный код
(`MainActivity.handleButtonPress`) намеренно запускает оба сканера
одновременно из одного нажатия; ни на нативной, ни на Dart-стороне нет
взаимного мьютекса между UHF- и штрихкод-потоками; хотя бы один экран
(`identifications_step_page.dart`) слушает оба одновременно и
различает поля только по длине строки, не по источнику скана.

Задал 2 уточняющих вопроса. Ответы:
- **Пункт 9**: баг реально воспроизводится, но не через карточку, а через
  массовое взвешивание — попросил перепроверить, действительно ли
  `_findTodayWeighing` вызывается всегда, а не только в одиночном сценарии.
- **Пункт 10**: реальный сценарий пользователя — было настроено **только
  UHF**, но запустился QR (не комбинированный режим). Попросил короткую
  проверку, без глубины — сам перепроверит на реальном устройстве позже.

## Промпт 12 (пользователь)

> сводку не нужно. Пора разрабаывать. Начинай решать задачи по очереди

## Ответ 12 (ассистент)

Реализовал 7 готовых `4-tasks/` по порядку (пункты 1, 2, 3, 4, 7, 8, 9;
пункты 5 и 6 пропущены ранее, пункт 10 оставлен открытым вопросом):

1. **SHEEP-4-1** (QR визуальная бирка) — `resolveScannedNumber()` вынесена
   из `scanner_widget.dart`, добавлена кнопка сканирования в
   `identifications_step_page.dart`.
2. **SHEEP-4-2** (INV «Отделение не указано») — новый локализационный ключ
   во всех `.arb`, `_computeSections` в `inventory_report_details_view.dart`
   и `_UnknownNumbersWrap` в `inventory_accordion_list_widget.dart`
   доработаны; найден и обновлён второй потребитель того же виджета
   (`inventory_scan_step_page.dart`), не упомянутый в исходной спеке.
3. **SHEEP-4-3** (тап-переход в карточку животного из отчётов) — новый
   общий тип `EventReportAnimalEntry`, тап-переход добавлен в
   `event_report_template.dart` и прокинут через
   movement/vaccination/disposal report cubit'ы и вьюхи.
4. **SHEEP-4-4** (компактный номер) — новый общий виджет
   `HighlightedNumberText`, заменил 2 приватных дубля.
5. **SHEEP-4-7** (RFID регион/мощность) — `region_selector_widget.dart`
   дополнен реальными странами/регионами для всех 10 `DeviceRegion`;
   убран cosmetic-remap в `scan_tcp.dart`/`scan_grp_tcp.dart` (после
   чтения нативного Java SDK и подтверждения, что `TCPUHF` действительно
   применяет регион через SDK-вызов, а `GrpTCPUHF` — no-op на прошивке
   устройства); RFID-03 (`com.rfid.API`/CRP-протокол) подтверждён как
   отдельный SDK без функции управления частотой вообще — задокументирован
   как отложенный, не стал писать вслепую нативный код.
6. **SHEEP-4-8** (валидация названия фермы + клавиатура + двойной тап) —
   `Validator.isNotBlank`, `FarmNameStepPage` обёрнут в `Form`,
   `FarmCreateCubit.saveFarm()` получил guard от повторного вызова
   (`isSubmitting`, потребовал `build_runner`).
7. **SHEEP-4-9** (дедуп взвешивания при массовом сценарии) — перепроверил
   по требованию пользователя, нашёл реальный root cause: не в условии
   `if (animal != null)`, а в том, что `AnimalsDao._toListAnimalsWithDetails`
   не подтягивал `animalWeighings` для search/mass-флоу; исправлено в DAO,
   написан реальный (не мокнутый) integration-тест на in-memory Drift БД.

После каждого изменения в `lib/`/`packages/sheep_farm_database/lib/`
прогонялся полный `flutter test` (1249/1249) — по требованию корневого
`CLAUDE.md`. Каждая задача закрыта записью в `5-results/`.

## Промпт 13 (пользователь)

> все отлично. Нужно еще внести правку по задаче 2. Нужно добавить переход
> и в отчете об инвентаризации тоже

## Ответ 13 (ассистент)

Распространил тап-переход в карточку животного (уже сделанный в задаче 3
для отчётов перемещения/вакцинации/выбытия) на отчёт инвентаризации и на
экран живого сканирования сессии (второй потребитель того же общего
виджета).

Код: `InventoryAgeGroupSection`/`InventoryAbsentEntry`/
`InventoryForeignKnownEntry` получили `animalId`; новый хелпер
`_tappableRow` в `inventory_accordion_list_widget.dart` оборачивает
`_TagNumberRow`/`_AbsentAnimalRow`/`_KnownForeignRow` в `InkWell` с
переходом на `Routes.animalDetailsFromSearch`; `_computeSections` в
`inventory_report_details_view.dart` и в `inventory_scan_step_page.dart`
прокидывают `animal.animalId`; `_UnknownNumbersWrap` осознанно не тронут
(нет `animalId`). По ходу правки исправил внесённую самим собой
рассинхронизацию скобок в `_KnownForeignRow`. `flutter analyze` чисто,
`inventory_report_details_cubit_test.dart` и полный `flutter test`
(1249/1249) проходят.

SDLC: заведён `BT-25` (дополнение к `R17`); `UC-307`/`UC-308` entombed с
`superseded-by: UC-317`/`UC-318`, новые `UC-317`/`UC-318` написаны с
TARGET-разделом про тап-переход; индекс `2-specs/use-cases/README.md`
обновлён; `4-tasks/SHEEP-4-2-INV-FOREIGN-TAG-DEPARTMENT.md` дополнен
разделом про новый объём; `5-results/SHEEP-4-2-INV-FOREIGN-TAG-DEPARTMENT.md`
дополнен соответствующим разделом.

## Промпт 14 (пользователь)

> @lib/pages/scanner_settings/widgets/region_selector_widget.dart
> _regionLabels тут нужно было перевод сделать. Ты захардкодил все на
> русском (раньше перевод был не нужен, потому что были одни числа и
> сокращения стран)
>
> И страны перенеси вперед. Пусть сначала будут страны а потом диапозоны.
> Без сокращения. То есть пусть будет например:
>
> Китай (920–925 МГц)

## Ответ 14 (ассистент)

Признал нарушение проектной конвенции «никогда не хардкодить текст
инлайн» (`.claude/rules/ui-architecture.md`) — в задаче 7
(`SHEEP-4-7`) `_regionLabels` была статической Dart-константой с
русским текстом, без реального перевода на 9 языков приложения.

Исправил: 10 новых ключей `scanner_region_*` (по одному на каждое
значение `DeviceRegion`) добавлены с реальным переводом во все 9
`.arb`-файлов, в новом формате «Страна (диапазон МГц)» без технических
сокращений `CN`/`FCC`/`ETSI` (кроме случаев, где сама аббревиатура —
часть названия стандарта, а не код страны, напр. «США / страны
стандарта FCC»). `_regionLabels` переведена со статической константы на
метод, принимающий `AppLocalizations`; `_getDisplayString` получил
`BuildContext`. `flutter gen-l10n`, `flutter analyze`, полный
`flutter test` (1249/1249) — без замечаний. Обновлён
`5-results/SHEEP-4-7-RFID-SETTINGS.md` разделом о находке и
исправлении; новый `BT`/`UC` не заводился — правка не меняет ни одного
документированного в `UC-314` поведенческого сценария (та спека — про
применение настроек на устройство, не про источник текста лейблов),
это исправление в периметре уже принятой задачи 7.

**Пункт 9, повторная проверка** (сам, без агента, прочитал
`weigh_animal_cubit.dart` целиком): нашёл точную причину — не в условии
`if (animal != null)`, а в том, откуда берётся `AnimalWithDetails`.
Одиночная карточка грузит животное через `AnimalsDao.getAllAnimalsWithDetailsByFilters`
(подтягивает `animalWeighings`), массовое взвешивание — через
`AnimalsDao._toListAnimalsWithDetails` (вызывается из
`searchAllAnimalsWithDetailsByNumbersAndName`), которая `animalWeighings`
**не подтягивает вообще** — из-за этого `_findTodayWeighing` в массовом
сценарии всегда возвращает `null`, всегда `insert`, никогда `update`.
Оформил по пайплайну: `BT-24-PLANNING-ANIMAL-WEIGH-DEDUP-MASS` (`R13`),
`UC-113` → `UC-316` (entomb+supersede, TARGET — добавить `animalWeighings`
в `_toListAnimalsWithDetails`, единая точка фикса), индекс обновлён,
`4-tasks/SHEEP-4-9-WEIGH-DEDUP-MASS.md` заведена.

**Пункт 10, короткая проверка** (сам, без агента, только по прямому
запросу пользователя): пустая `buttonActionsMap` на старте по умолчанию
запускает только UHF, не QR — не объясняет находку пользователя; настройки
всегда пушатся в нативный код целиком (не частично) — тоже не объясняет.
Дальше не копал по прямой просьбе о краткости. BT/UC/task не заводятся —
пользователь перепроверит на реальном устройстве самостоятельно, находки
зафиксированы в `SHEEP-4-clarifications.md` как открытый вопрос.

---

## Итог — ключевые созданные файлы

Вопросы/противоречия по каждому пункту тикета — отдельно от этого лога, в
[`SHEEP-4-clarifications.md`](SHEEP-4-clarifications.md) (тот же каталог).

### SDLC — планирование (`1-business-tasks/planning/`)

`BT-18` (QR-бирка), `BT-19` (INV «Отделение не указано»), `BT-20` (тап-переход
в отчётах), `BT-21` (компактный номер), `BT-22` (RFID регион/мощность),
`BT-23` (валидация имени фермы), `BT-24` (дедуп взвешивания),
`BT-25` (тап-переход в отчёте инвентаризации, дополнение к `BT-19`/`BT-20`).

### SDLC — спеки (`2-specs/`)

Use-cases (актуальные, живые): `UC-306`, `UC-309`, `UC-310`, `UC-311`,
`UC-312`, `UC-313`, `UC-314`, `UC-315`, `UC-316`, `UC-317`, `UC-318`.
Новое событие: `EVT-148`.

### SDLC — задачи и результаты (`4-tasks/`, `5-results/`)

По одному файлу на каждый реализованный пункт: `SHEEP-4-1` (QR-бирка),
`SHEEP-4-2` (INV «Отделение не указано» + тап-переход), `SHEEP-4-3`
(тап-переход в карточку животного из отчётов), `SHEEP-4-4` (компактный
номер), `SHEEP-4-7` (RFID регион/мощность), `SHEEP-4-8` (валидация имени
фермы), `SHEEP-4-9` (дедуп взвешивания).

### Код приложения — новые файлы

- `lib/widgets/text/highlighted_number_text.dart` — общий виджет компактного
  номера, заменил 2 приватных дубля (`SHEEP-4-4`).

Остальная работа — правки уже существующих файлов (список — в
соответствующих `5-results/`).

---

"""Workflow prompts the server exposes to the host (Claude Code).

These are the "how to drive the process" guides. The host reads one, then uses
the server's tools to carry it out — the intelligence (asking the human,
sequencing) stays in the host; the law + audit stay in the tools.
"""

RUN_PASS = """\
# Прогон одного цикла SDLC

Ты ведёшь один проход пайплайна над деревом `sdlc/`. Закон (кратко): артефакты
**заморожены** — «изменить» значит выпустить новый id и **похоронить** старый;
in-place правится только PRD (со снапшотом в history/); проход **предлагает**, а
человек **утверждает**.

Порядок:

1. **Впитать данные.** Если есть новые сигналы/наблюдения — уточни у человека,
   что именно изменилось. Ничего не выдумывай: неизвестное записывай как
   открытый вопрос.
2. **PRD-дельта.** Если меняется намерение продукта — определи, какие `R{n}`
   добавить (`prd_add_requirement`) или депрекейтнуть (`prd_deprecate_requirement`).
   Депрекейт — в place, новый `R{n}` — на замену. Не переписывай смысл требования.
3. **Предложи план дельты** человеку ЯВНО, до записи: какие артефакты будут
   созданы, какие похоронены, и какое изменение PRD это оплачивает. Жди «да».
4. **Выпусти артефакты** сверху вниз (модуль → акторы/сущности/события →
   use-cases) через `*_create`. Каждый id, что цитируешь, должен уже существовать
   (`resolve_id`).
5. **Похорони устаревшее** через `entomb_artifact` (или передав `supersede_of` в
   `*_create`), указав, чем заменено.
6. **Проверь** дерево `validate_tree` — коллизии id недопустимы; висячие/стейл
   цитаты вынеси человеку на ревью, не хорони автоматически.

Каждый вызов инструмента уже пишется в аудит и транскрипт — отдельно логировать
не нужно. Историю смотри `audit_history`.
"""

NEW_FEATURE = """\
# От запроса человека до задачи на реализацию

Человек просит фичу/правку. Проведи её по стадиям, задавая вопросы там, где
поведение новое или неоднозначное.

1. **Пойми запрос.** Задай уточняющие вопросы (акторы, сущности, ожидаемый
   результат, крайние случаи). Открытые вопросы фиксируй, не угадывай.
2. **Требование.** Найди `R{n}`, которому служит фича (`read_prd`). Если такого
   нет — сначала PRD: `prd_add_requirement`, затем цитируй новый `R{n}`.
3. **Бизнес-задача.** `business_task_create` (kind=planning), процитировав
   `requirements`. Опиши текущее состояние (из кода) и что делать.
4. **Спеки.** Если фича новая для модуля — `module_create` (если модуля нет),
   затем `actor_create`/`entity_create`/`event_create`, затем `use_case_create`
   на каждый достижимый сценарий (успех/ошибка/отказ — разные `RESULT`).
5. **Задача на код** пишется под спеку; реализацию веди по промпту `tdd_task`.
6. `validate_tree` в конце.

Ничего не переписывай in-place (кроме PRD) — только новые артефакты + захоронение.
"""

TDD_TASK = """\
# TDD-цикл для задачи по коду мобильного приложения

Разработка кода приложения (репозиторий `agro_system`) — строго через
red → green → refactor. Правило проекта: после изменений в `lib/` обязателен
`flutter test`.

1. **Red.** Сначала напиши падающий тест на желаемое поведение (из `Критерии
   приёмки` use-case'а). Привяжи его к спеке: `group('UC-{id} — …')`. Запусти —
   убедись, что падает по нужной причине.
2. **Green.** Минимальная реализация, чтобы тест позеленел. Запусти `flutter
   test` — всё зелёное.
3. **Refactor.** Приведи код к конвенциям проекта (тема/типографика, cubit/bloc,
   go_router, freezed) — тесты остаются зелёными.
4. **Результат.** Зафиксируй, что сделано и что отложено, в стадии `5-results/`
   (файл-зеркало задачи по имени).
5. **PR и сборка.** Когда готово — `open_pull_request`, затем при необходимости
   `build_and_deliver_apk` (доставка в Telegram). Реальные вызовы включаются
   после настройки ключей — см. docs/SETUP.md.

Не считай задачу выполненной, если тесты не запускались после последнего
изменения или падают.
"""
